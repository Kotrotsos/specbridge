[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = ["npm ci", "npx prisma generate", "npx next build"]

[phases.deploy]
cmds = []

[start]
cmd = "npx prisma db push --accept-data-loss && npm start"
